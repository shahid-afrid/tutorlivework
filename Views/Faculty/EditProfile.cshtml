@model TutorLiveMentor.Models.FacultyEditProfileViewModel

@{
    ViewData["Title"] = "Edit Faculty Profile";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Faculty Profile - TutorLiveMentor</title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:700,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <style>
        :root {
            --royal-blue: #274060;
            --warm-gold: #FFC857;
            --cream: #F9FAF2;
            --slate: #CED3DC;
            --card-shadow: rgba(39,64,96,0.13);
            --success-green: #28a745;
            --error-red: #dc3545;
            --faculty-purple: #6f42c1;
        }

        body {
            min-height: 100vh;
            margin: 0;
            padding: 20px 0;
            font-family: 'Montserrat', Arial, sans-serif;
            background: linear-gradient(rgba(249, 250, 242, 0.15), rgba(255, 200, 87, 0.10)),
            url('@Url.Content("~/images/RGMCET-BACKGROUND.jpg")') center center/cover no-repeat fixed;
            position: relative;
        }

        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: 0;
            display: block;
            background: linear-gradient(135deg, var(--cream) 60%, var(--warm-gold) 100%);
            opacity: 0.20;
            pointer-events: none;
        }

        .main-container {
            position: relative;
            z-index: 1;
            max-width: 600px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .nav-link {
            position: fixed;
            top: 20px;
            left: 20px;
            background: rgba(249,250,242,0.9);
            color: var(--royal-blue);
            padding: 10px 20px;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            border: 2px solid var(--slate);
            backdrop-filter: blur(8px);
            transition: all 0.3s ease;
            z-index: 100;
        }

        .nav-link:hover {
            background: var(--warm-gold);
            color: var(--royal-blue);
            transform: translateY(-2px);
            text-decoration: none;
        }

        .dashboard-header {
            background: rgba(249,250,242,0.92);
            box-shadow: 0 8px 32px var(--card-shadow);
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            backdrop-filter: blur(12px);
            margin-bottom: 40px;
            border: 2px solid var(--slate);
        }

        .dashboard-header h2 {
            font-size: 2.5em;
            font-weight: 700;
            color: var(--royal-blue);
            margin: 0;
            letter-spacing: 1.2px;
        }

        .form-card {
            background: rgba(249,250,242,0.95);
            border-radius: 20px;
            padding: 40px;
            backdrop-filter: blur(12px);
            border: 2px solid var(--slate);
            box-shadow: 0 8px 32px var(--card-shadow);
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-label {
            display: block;
            font-weight: 600;
            font-size: 0.9em;
            color: var(--royal-blue);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .form-control {
            width: 100%;
            padding: 15px;
            border: 2px solid var(--slate);
            border-radius: 10px;
            font-size: 1.1em;
            background: rgba(255, 255, 255, 0.8);
            transition: all 0.3s ease;
            box-sizing: border-box;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--faculty-purple);
            background: rgba(255, 255, 255, 0.95);
            box-shadow: 0 0 0 3px rgba(111, 66, 193, 0.1);
        }

        .text-danger {
            color: var(--error-red);
            font-size: 0.9em;
            margin-top: 5px;
            display: block;
        }

        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .glass-btn {
            background: linear-gradient(135deg, var(--faculty-purple) 0%, var(--warm-gold) 100%);
            color: white;
            border: none;
            border-radius: 25px;
            padding: 15px 30px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            box-shadow: 0 8px 25px var(--card-shadow);
            border: 2px solid transparent;
            transition: all 0.3s ease;
        }

        .glass-btn:hover {
            background: linear-gradient(135deg, var(--warm-gold) 0%, var(--faculty-purple) 100%);
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(111, 66, 193, 0.4);
            text-decoration: none;
            color: white;
        }

        .glass-btn.secondary {
            background: linear-gradient(135deg, var(--slate) 0%, #95a5a6 100%);
        }

        .glass-btn.secondary:hover {
            background: linear-gradient(135deg, #95a5a6 0%, var(--slate) 100%);
        }

        @@media (max-width: 768px) {
            .main-container {
                padding: 0 15px;
            }

            .dashboard-header {
                padding: 30px 20px;
                margin-bottom: 30px;
            }

            .dashboard-header h2 {
                font-size: 2em;
            }

            .form-card {
                padding: 25px 20px;
            }

            .action-buttons {
                flex-direction: column;
                align-items: center;
            }

            .glass-btn {
                width: 100%;
                max-width: 250px;
                justify-content: center;
            }

            .nav-link {
                position: static;
                display: inline-block;
                margin-bottom: 20px;
            }
        }
    
        /* App Footer Styling */
        .app-footer {
            background: rgba(249,250,242,0.95);
            backdrop-filter: blur(12px);
            border-top: 3px solid var(--slate, #CED3DC);
            box-shadow: 0 -8px 32px rgba(39,64,96,0.13);
            padding: 30px 20px;
            margin-top: 50px;
            text-align: center;
            font-family: 'Montserrat', Arial, sans-serif;
            color: #274060;
            position: relative;
            border-radius: 20px 20px 0 0;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .footer-line {
            margin: 8px 0;
            line-height: 1.6;
        }

        .footer-line.guidance { font-size: 1em; font-weight: 600; color: #444;
            margin-bottom: 12px;
        }

        .footer-line.team {
            font-size: 0.95em;
            font-weight: 500;
            color: #444;
        }

        .footer-line.copyright {
            font-size: 0.9em;
            font-weight: 600;
            color: #274060;
            margin-top: 15px;
            padding-top: 15px;
            border-top: 2px solid rgba(39,64,96,0.1);
        }

        .footer-highlight { color: #274060;
            font-weight: 700;
        }

        .footer-name { color: #274060;
            font-weight: 600;
        }

        @@media (max-width: 768px) {
            .app-footer {
                padding: 20px 15px;
                margin-top: 30px;
            }

            .footer-line.guidance {
                font-size: 0.9em;
            }

            .footer-line.team {
                font-size: 0.85em;
            }

            .footer-line.copyright {
                font-size: 0.8em;
            }
        }

        .password-toggle {
            position: relative;
        }

        .password-toggle-icon {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: var(--slate);
            transition: color 0.3s ease;
        }

        .password-toggle-icon:hover {
            color: var(--faculty-purple);
        }

        .divider {
            margin: 40px 0 30px;
            text-align: center;
            position: relative;
        }

        .divider::before {
            content: "";
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, var(--slate), transparent);
        }

        .divider-text {
            background: rgba(249,250,242,0.95);
            padding: 0 20px;
            position: relative;
            display: inline-block;
            color: var(--royal-blue);
            font-weight: 600;
            font-size: 0.9em;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .info-message {
            background: rgba(111, 66, 193, 0.1);
            border-left: 4px solid var(--faculty-purple);
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 25px;
            font-size: 0.9em;
            color: var(--royal-blue);
        }

        .info-message i {
            margin-right: 8px;
            color: var(--faculty-purple);
        }

        .success-message {
            background: rgba(40, 167, 69, 0.1);
            border-left: 4px solid var(--success-green);
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 25px;
            font-size: 0.95em;
            color: var(--success-green);
            font-weight: 600;
        }

        .success-message i {
            margin-right: 8px;
        }

        .read-only-field {
            background: rgba(206, 211, 220, 0.2);
            border: 2px solid var(--slate);
            cursor: not-allowed;
        }

        .department-badge {
            display: inline-block;
            background: linear-gradient(135deg, var(--faculty-purple), var(--warm-gold));
            color: white;
            padding: 8px 20px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 1em;
            letter-spacing: 0.5px;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <a href="@Url.Action("Profile", "Faculty")" class="nav-link"><i class="fas fa-arrow-left"></i> Back to Profile</a>
    
    <div class="main-container">
        <div class="dashboard-header">
            <h2>Edit Faculty Profile</h2>
        </div>

        <div class="form-card">
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="success-message">
                    <i class="fas fa-check-circle"></i> @TempData["SuccessMessage"]
                </div>
            }

            <form asp-action="EditProfile" method="post">
                <input type="hidden" asp-for="FacultyId" />
                <input type="hidden" asp-for="Department" />

                <div class="form-group">
                    <label asp-for="Name" class="form-label">Faculty Name</label>
                    <input asp-for="Name" class="form-control" placeholder="Enter your full name" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Email" class="form-label">Email Address</label>
                    <input asp-for="Email" class="form-control" placeholder="Enter your email address" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label class="form-label"><i class="fas fa-building"></i> Department</label>
                    <div>
                        <span class="department-badge">@Model.Department</span>
                    </div>
                    <small style="color: #666; margin-top: 8px; display: block;">
                        <i class="fas fa-info-circle"></i> Department cannot be changed. Contact admin if needed.
                    </small>
                </div>

                <!-- Password Change Section -->
                <div class="divider">
                    <span class="divider-text"><i class="fas fa-key"></i> Change Password (Optional)</span>
                </div>

                <div class="info-message">
                    <i class="fas fa-info-circle"></i>
                    Leave password fields empty if you don't want to change your password.
                </div>

                <div class="form-group">
                    <label asp-for="CurrentPassword" class="form-label">Current Password</label>
                    <div class="password-toggle">
                        <input asp-for="CurrentPassword" class="form-control" id="currentPassword" placeholder="Enter your current password" />
                        <i class="fas fa-eye password-toggle-icon" onclick="togglePassword('currentPassword', this)"></i>
                    </div>
                    <span asp-validation-for="CurrentPassword" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="NewPassword" class="form-label">New Password</label>
                    <div class="password-toggle">
                        <input asp-for="NewPassword" class="form-control" id="newPassword" placeholder="Enter new password (min. 6 characters)" />
                        <i class="fas fa-eye password-toggle-icon" onclick="togglePassword('newPassword', this)"></i>
                    </div>
                    <span asp-validation-for="NewPassword" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="ConfirmPassword" class="form-label">Confirm New Password</label>
                    <div class="password-toggle">
                        <input asp-for="ConfirmPassword" class="form-control" id="confirmPassword" placeholder="Confirm your new password" />
                        <i class="fas fa-eye password-toggle-icon" onclick="togglePassword('confirmPassword', this)"></i>
                    </div>
                    <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                </div>

                <div class="action-buttons">
                    <button type="submit" class="glass-btn">
                        <i class="fas fa-save"></i> Save Changes
                    </button>
                    <a asp-action="Profile" class="glass-btn secondary">
                        <i class="fas fa-times"></i> Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>

    <footer class="app-footer">
        <div class="footer-content">
            <div class="footer-line guidance">
                Developed under the guidance of <span class="footer-highlight">Dr. P. Penchala Prasad</span>, Associate Professor, CSE(DS), RGMCET
            </div>
            <div class="footer-line team">
                With team <span class="footer-name">Mr. S. Md. Shahid Afrid</span> (23091A32D4) & <span class="footer-name">Ms. G. Veena</span> (23091A32H9)
            </div>
            <div class="footer-line copyright">
                &copy; All Rights Reserved @@2025
            </div>
        </div>
    </footer>

    <script>
        function togglePassword(inputId, icon) {
            const input = document.getElementById(inputId);
            if (input.type === 'password') {
                input.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                input.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }
    </script>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
</body>
</html>